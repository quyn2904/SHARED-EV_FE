import { Button } from "@/components";

const HomePage = () => {
  return (
    <div>
      <Button size="lg" variant="outline">
        Ahihi
      </Button>
    </div>
  );
};

export default HomePage;
